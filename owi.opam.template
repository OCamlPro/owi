available: (arch = "x86_32" | arch = "x86_64" | arch = "arm64") & os != "win32"
depexts: [
  ["llvm" "clang" "lld" "wabt"]  {os-family = "debian"}
  ["llvm" "wabt"]  {os-family = "homebrew"}
]
pin-depends: [
  [ "encoding" "git+https://github.com/formalsec/encoding"]
]

