available: (arch = "x86_32" | arch = "x86_64" | arch = "arm64") & os != "win32"
depexts: [
  ["lld" "wabt"]  {os-family = "debian"}
  ["wabt"]  {os-family = "homebrew"}
]
pin-depends: [
  [ "encoding.dev" "git+https://github.com/formalsec/encoding"]
]

